{% extends 'base.html.twig' %}

{% block title %}{{blog.getTitle()}}{% endblock %}

{% block body %}
  {{parent()}}
  <div class="p-3">
  <div>
  {{ blog.content|raw }}
  </div>
  <p>Publié le {{ blog.getDate()|date('d/m/y') }}</p>
  </div>
{% endblock %}
