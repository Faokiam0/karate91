{% extends 'base.html.twig' %}

{% block title %}Nos Partenaires{% endblock %}

{% block body %}
  {{parent()}}

<div class="p-3 m-5">
    {% for partner in partners %}
    <a href="{{partner.link}}"><img src="{{partner.icon}}" alt="{{patner.name}}"></a>
    {% endfor %}
</div>
{% endblock %}
