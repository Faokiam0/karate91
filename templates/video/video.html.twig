{% extends 'base.html.twig' %}

{% block title %}CSKS91 - Page d'accueil{% endblock %}

{% block body %}
  {{parent()}}
  <h1>{{ video.title }}</h1>
  <iframe width="420" height="315" src="{{ video.link }}">
  </iframe>
  <p>{{ video.description}}</p>
  <p>Publié le {{ video.getDate()|date('d/m/y') }}</p>
{% endblock %}
